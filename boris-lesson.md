I just corrected you or pointed out a mistake. Do the following:

1. Identify the ROOT CAUSE, not the surface symptom. Ask yourself: what assumption or missing knowledge led to this error?
2. Write a concise, actionable rule that prevents this CLASS of mistake (not just this instance).
3. Check `LESSONS.md` for existing rules that cover the same territory. If one exists, sharpen it rather than adding a duplicate.
4. Append the rule to `LESSONS.md` using the format:
   `- **[YYYY-MM-DD]** category: rule`
   Categories: code-style | architecture | testing | tooling | domain | process
5. If this lesson is universal (applies across all projects, not domain-specific), also append to `~/.claude/LESSONS.md`. Create it if needed.
6. Confirm: state what you added, which file(s), and why you classified it at that scope (project vs global).

Correction context: $ARGUMENTS
